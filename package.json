{
  "name": "katur",
  "version": "1.0.0",
  "description": "",
  "main": "server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "server": "cd server && nodemon app.js",
    "start": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "i": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kbmelo/katur.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kbmelo/katur/issues"
  },
  "homepage": "https://github.com/kbmelo/katur#readme",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
